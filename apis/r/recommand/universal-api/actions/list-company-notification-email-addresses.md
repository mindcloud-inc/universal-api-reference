# Recommand: List Company Notification Email Addresses

Retrieves company notification email addresses from Recommand.

```
GET https://connect.mindcloud.co/v1/universal/recommand/latest/actions/list-company-notification-email-addresses
```

Authenticate with `Authorization: Bearer $MINDCLOUD_API_KEY` and pass a Recommand `connectionId` ([setup](../authentication.md)).

This action also supports [pagination](../pagination.md) (`limit`, `offset`).

## Example request

```bash
curl -X GET "https://connect.mindcloud.co/v1/universal/recommand/latest/actions/list-company-notification-email-addresses?connectionId=$CONNECTION_ID&limit=25&offset=0&companyid=string" \
  -H "Authorization: Bearer $MINDCLOUD_API_KEY"
```

```js
const params = new URLSearchParams({
  connectionId,
  limit: '25',
  offset: '0',
  "companyid": "string"
});

const response = await fetch(`https://connect.mindcloud.co/v1/universal/recommand/latest/actions/list-company-notification-email-addresses?${params}`, {
  headers: {
    Authorization: `Bearer ${process.env.MINDCLOUD_API_KEY}`
  }
});

const { success, data } = await response.json();
```

## Inputs

Arguments are sent as query string parameters ([conventions](../arguments.md)).

| Key | Type | Required | Description |
| --- | --- | --- | --- |
| `companyid` | string | yes | companyId parameter. |

## Response

```json
{
  "success": true,
  "data": [
    {
      "notificationEmailAddresses": [
        {
          "companyId": "ava@example.com",
          "createdAt": "ava@example.com",
          "email": "ava@example.com",
          "id": "ava@example.com",
          "includeAutoGeneratedPdfIncoming": true,
          "includeAutoGeneratedPdfOutgoing": true,
          "includeDocumentJsonIncoming": true,
          "includeDocumentJsonOutgoing": true,
          "notifyIncoming": true,
          "notifyOutgoing": true,
          "updatedAt": "ava@example.com"
        }
      ],
      "success": true
    }
  ],
  "meta": {}
}
```

### Response fields

| Key | Type | Description |
| --- | --- | --- |
| `notificationEmailAddresses` | array<object> |  |
| `notificationEmailAddresses[].companyId` | string |  |
| `notificationEmailAddresses[].createdAt` | string |  |
| `notificationEmailAddresses[].email` | string |  |
| `notificationEmailAddresses[].id` | string |  |
| `notificationEmailAddresses[].includeAutoGeneratedPdfIncoming` | boolean |  |
| `notificationEmailAddresses[].includeAutoGeneratedPdfOutgoing` | boolean |  |
| `notificationEmailAddresses[].includeDocumentJsonIncoming` | boolean |  |
| `notificationEmailAddresses[].includeDocumentJsonOutgoing` | boolean |  |
| `notificationEmailAddresses[].notifyIncoming` | boolean |  |
| `notificationEmailAddresses[].notifyOutgoing` | boolean |  |
| `notificationEmailAddresses[].updatedAt` | string |  |
| `success` | boolean |  |

## Native endpoint

Through the native Recommand API, this operation is `GET /api/v1/companies/:companyId/notification-email-addresses` (base URL `https://app.recommand.eu`). The Universal API call above is translated to it by MindCloud, including authentication and pagination. See the [native action reference](../../native-api/actions/list-company-notification-email-addresses.md) for the provider-specific parameters and requirements.

