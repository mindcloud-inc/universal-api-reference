# Recommand: Get Company Notification Email Address

Retrieves a company notification email address from Recommand.

```
GET https://connect.mindcloud.co/v1/universal/recommand/latest/actions/get-company-notification-email-address
```

Authenticate with `Authorization: Bearer $MINDCLOUD_API_KEY` and pass a Recommand `connectionId` ([setup](../authentication.md)).

## Example request

```bash
curl -X GET "https://connect.mindcloud.co/v1/universal/recommand/latest/actions/get-company-notification-email-address?connectionId=$CONNECTION_ID&companyid=string&notificationemailaddressid=ava%40example.com" \
  -H "Authorization: Bearer $MINDCLOUD_API_KEY"
```

```js
const params = new URLSearchParams({
  connectionId,
  "companyid": "string",
  "notificationemailaddressid": "ava@example.com"
});

const response = await fetch(`https://connect.mindcloud.co/v1/universal/recommand/latest/actions/get-company-notification-email-address?${params}`, {
  headers: {
    Authorization: `Bearer ${process.env.MINDCLOUD_API_KEY}`
  }
});

const { success, data } = await response.json();
```

## Inputs

Arguments are sent as query string parameters ([conventions](../arguments.md)).

| Key | Type | Required | Description |
| --- | --- | --- | --- |
| `companyid` | string | yes | companyId parameter. |
| `notificationemailaddressid` | string | yes | notificationEmailAddressId parameter. |

## Response

```json
{
  "success": true,
  "data": [
    {
      "notificationEmailAddress": {
        "companyId": "ava@example.com",
        "createdAt": "ava@example.com",
        "email": "ava@example.com",
        "id": "ava@example.com",
        "includeAutoGeneratedPdfIncoming": true,
        "includeAutoGeneratedPdfOutgoing": true,
        "includeDocumentJsonIncoming": true,
        "includeDocumentJsonOutgoing": true,
        "notifyIncoming": true,
        "notifyOutgoing": true,
        "updatedAt": "ava@example.com"
      },
      "success": true
    }
  ],
  "meta": {}
}
```

### Response fields

| Key | Type | Description |
| --- | --- | --- |
| `notificationEmailAddress` | object |  |
| `notificationEmailAddress.companyId` | string |  |
| `notificationEmailAddress.createdAt` | string |  |
| `notificationEmailAddress.email` | string |  |
| `notificationEmailAddress.id` | string |  |
| `notificationEmailAddress.includeAutoGeneratedPdfIncoming` | boolean |  |
| `notificationEmailAddress.includeAutoGeneratedPdfOutgoing` | boolean |  |
| `notificationEmailAddress.includeDocumentJsonIncoming` | boolean |  |
| `notificationEmailAddress.includeDocumentJsonOutgoing` | boolean |  |
| `notificationEmailAddress.notifyIncoming` | boolean |  |
| `notificationEmailAddress.notifyOutgoing` | boolean |  |
| `notificationEmailAddress.updatedAt` | string |  |
| `success` | boolean |  |

## Native endpoint

Through the native Recommand API, this operation is `GET /api/v1/companies/:companyId/notification-email-addresses/:notificationEmailAddressId` (base URL `https://app.recommand.eu`). The Universal API call above is translated to it by MindCloud, including authentication. See the [native action reference](../../native-api/actions/get-company-notification-email-address.md) for the provider-specific parameters and requirements.

