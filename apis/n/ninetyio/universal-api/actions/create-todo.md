# Ninety.io: Create To-Do

Creates a new to-do in Ninety.io.

```
POST https://connect.mindcloud.co/v1/universal/ninetyio/latest/actions/create-todo
```

Authenticate with `Authorization: Bearer $MINDCLOUD_API_KEY` and pass a Ninety.io `connectionId` ([setup](../authentication.md)).

## Example request

```bash
curl -X POST "https://connect.mindcloud.co/v1/universal/ninetyio/latest/actions/create-todo" \
  -H "Authorization: Bearer $MINDCLOUD_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
  "connectionId": "$CONNECTION_ID",
  "title": "string"
}'
```

```js
const response = await fetch('https://connect.mindcloud.co/v1/universal/ninetyio/latest/actions/create-todo', {
  method: 'POST',
  headers: {
    Authorization: `Bearer ${process.env.MINDCLOUD_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    connectionId,
    "title": "string"
  })
});

const { success, data } = await response.json();
```

## Inputs

Arguments are sent as JSON body fields ([conventions](../arguments.md)).

| Key | Type | Required | Description |
| --- | --- | --- | --- |
| `title` | string | yes |  |
| `description` | string | no | The description of the To-Do |
| `dueDate` | date | no | Due date in YYYY-MM-DD format |
| `teamId` | string | no | The Id of the team to assign this To-Do to |
| `repeat` | string | no | Recurrence pattern for the To-Do |
| `userId` | string | no | Id of the user to assign the To-Do to |

## Response

```json
{
  "success": true,
  "data": [
    {
      "archived": true,
      "archivedByUserId": {},
      "archivedDate": {},
      "autoGenerated": true,
      "companyId": "string",
      "completed": true,
      "completedByUserId": {},
      "completedDate": {},
      "createdBy": {},
      "createdByUserId": "string",
      "createdDate": "string",
      "deleted": true,
      "deletedByUserId": {},
      "deletedDate": {},
      "description": "string",
      "dueDate": "string",
      "dueDateChanged": true,
      "followers": [
        "string"
      ],
      "Id": "string",
      "imported": true,
      "isPersonal": true,
      "milestoneId": {},
      "ordinal": 1,
      "originalDueDate": "string",
      "repeat": "string",
      "seriesId": {},
      "teamId": {},
      "title": "string",
      "userId": "string",
      "userOrdinal": 1,
      "Version": 1
    }
  ],
  "meta": {}
}
```

### Response fields

| Key | Type | Description |
| --- | --- | --- |
| `archived` | boolean |  |
| `archivedByUserId` | object |  |
| `archivedDate` | object |  |
| `autoGenerated` | boolean |  |
| `companyId` | string |  |
| `completed` | boolean |  |
| `completedByUserId` | object |  |
| `completedDate` | object |  |
| `createdBy` | object |  |
| `createdByUserId` | string |  |
| `createdDate` | string |  |
| `deleted` | boolean |  |
| `deletedByUserId` | object |  |
| `deletedDate` | object |  |
| `description` | string |  |
| `dueDate` | string |  |
| `dueDateChanged` | boolean |  |
| `followers[]` | string |  |
| `Id` | string |  |
| `imported` | boolean |  |
| `isPersonal` | boolean |  |
| `milestoneId` | object |  |
| `ordinal` | number |  |
| `originalDueDate` | string |  |
| `repeat` | string |  |
| `seriesId` | object |  |
| `teamId` | object |  |
| `title` | string |  |
| `userId` | string |  |
| `userOrdinal` | number |  |
| `Version` | number |  |

## Native endpoint

Through the native Ninety.io API, this operation is `POST /v1/todos` (base URL `https://api.public.ninety.io`). The Universal API call above is translated to it by MindCloud, including authentication. See the [native action reference](../../native-api/actions/create-todo.md) for the provider-specific parameters and requirements.

